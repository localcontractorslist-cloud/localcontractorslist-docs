site_name: Local Contractors List Docs
site_url: https://<your-project>.readthedocs.io

# Tell MkDocs that docs live in the repo root (not /docs)
docs_dir: .

theme:
  name: readthedocs

nav:
  - Home: index.md
