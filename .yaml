# .readthedocs.yaml
# Configuration file for Read the Docs

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

# Use MkDocs to build your Markdown docs
mkdocs:
  configuration: mkdocs.yml

# Set build requirements
python:
  install:
    - requirements: docs/requirements.txt
